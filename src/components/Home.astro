---
import SiteLayout from '../layouts/SiteLayout.astro';
import { solutions } from '../data/solutions';
import { solutionContent, type Locale } from '../i18n/ui';
import { useTranslations } from '../i18n/utils';

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
const t = useTranslations(locale);

// Merge the static solution shell with the localized content for this locale.
const localizedSolutions = solutions.map((p) => {
  const extra = solutionContent[locale][p.id] ?? solutionContent.en[p.id];
  return { ...p, ...extra };
});

---

<SiteLayout locale={locale}>
  <!-- ============================ HERO ============================ -->
  <header class="hero" id="top">
    <div class="wrap hero__inner">
      <code class="hero__cmd">
        <span class="p-user">gumbarros</span><span class="p-path">@portfolio:~$</span>
        <span data-type="whoami"></span><span class="cursor" aria-hidden="true"></span>
      </code>
      <h1 class="hero__title" set:html={t('hero.title')} />
      <p class="hero__lede" set:html={t('hero.lede')} />
      <div class="hero__ctas">
        <a class="btn btn--primary" href="#contact">
          <span>{t('hero.contact')}</span><span class="arrow">→</span>
        </a>
      </div>
    </div>
  </header>

  <!-- ============================ PORTFOLIO ============================ -->
  <section class="portfolio" id="portfolio">
    <div class="wrap portfolio__head reveal">
      <code class="cmd">gumbarros@portfolio:~$ {t('portfolio.cmd')}</code>
      <h2>{t('portfolio.headline')}</h2>
      <p>{t('portfolio.intro')}</p>
    </div>
    <div class="wrap portfolio__grid">
      {localizedSolutions.map((p) => (
        <article class="project-card reveal">
          <div class="project-card__top">
            <span class="project-card__type">{p.category}</span>
            {p.stars && <code>★ {p.stars}</code>}
          </div>
          <h3>{p.name}</h3>
          <p>{p.description}</p>
          <div class="project-card__footer">
            <span>{p.role}</span>
            {p.moreUrl ? (
              <a href={p.moreUrl} target="_blank" rel="noopener">{t('portfolio.visit')} <span class="arrow">↗</span></a>
            ) : (
              <a href={p.github} target="_blank" rel="noopener">{t('portfolio.details')} <span class="arrow">↗</span></a>
            )}
          </div>
        </article>
      ))}
    </div>
  </section>

  <!-- ============================ FEATURED PROJECT ============================ -->
  <section class="featured-project" id="solutions">
    <div class="wrap featured-project__inner reveal">
      <div class="featured-project__identity">
        <img src="https://gumbarros.com.br/casa-da-musica/logo.jpg" alt="Casa da Música" width="76" height="76" loading="lazy" decoding="async" />
        <span>{t('featuredProject.label')}</span>
      </div>
      <div class="featured-project__content">
        <h2>Casa da Música</h2>
        <p>{t('featuredProject.body')}</p>
        <a class="btn btn--primary" href="https://gumbarros.com.br/casa-da-musica" target="_blank" rel="noopener">
          <span>{t('featuredProject.visit')}</span><span class="arrow">↗</span>
        </a>
      </div>
    </div>
  </section>

  <!-- ============================ ABOUT ============================ -->
  <section class="about" id="about">
    <div class="wrap">
      <code class="about__head"><span class="p-user">gumbarros</span><span class="p-path">@portfolio:~$</span> {t('about.cmd')}</code>
      <h2 class="reveal">{t('about.headline')}</h2>
      <div class="about__story reveal">
        <p class="about__lead" set:html={t('about.body')} />
      </div>

      <div class="about__lower">
        <div class="career-terminal reveal">
          <div class="career-terminal__bar">
            <span><i></i><i></i><i></i></span>
            <code>/var/log/gumbarros</code>
            <span class="career-terminal__status">● {t('about.status')}</span>
          </div>
          <div class="career-terminal__body">
            <svg class="career-icons" aria-hidden="true">
              <symbol id="career-icon-0" viewBox="0 0 16 16"><path d="M8.211 2.047a.5.5 0 0 0-.422 0l-7.5 3.5a.5.5 0 0 0 .025.917l7.5 3a.5.5 0 0 0 .372 0L14 7.14V13a1 1 0 0 0-1 1v2h3v-2a1 1 0 0 0-1-1V6.739l.686-.275a.5.5 0 0 0 .025-.917zM8 8.46 1.758 5.965 8 3.052l6.242 2.913z"/><path d="M4.176 9.032a.5.5 0 0 0-.656.327l-.5 1.7a.5.5 0 0 0 .294.605l4.5 1.8a.5.5 0 0 0 .372 0l4.5-1.8a.5.5 0 0 0 .294-.605l-.5-1.7a.5.5 0 0 0-.656-.327L8 10.466zm-.068 1.873.22-.748 3.496 1.311a.5.5 0 0 0 .352 0l3.496-1.311.22.748L8 12.46z"/></symbol>
              <symbol id="career-icon-1" viewBox="0 0 16 16"><path d="M6.5 1A1.5 1.5 0 0 0 5 2.5V3H1.5A1.5 1.5 0 0 0 0 4.5v8A1.5 1.5 0 0 0 1.5 14h13a1.5 1.5 0 0 0 1.5-1.5v-8A1.5 1.5 0 0 0 14.5 3H11v-.5A1.5 1.5 0 0 0 9.5 1zm0 1h3a.5.5 0 0 1 .5.5V3H6v-.5a.5.5 0 0 1 .5-.5m1.886 6.914L15 7.151V12.5a.5.5 0 0 1-.5.5h-13a.5.5 0 0 1-.5-.5V7.15l6.614 1.764a1.5 1.5 0 0 0 .772 0M1.5 4h13a.5.5 0 0 1 .5.5v1.616L8.129 7.948a.5.5 0 0 1-.258 0L1 6.116V4.5a.5.5 0 0 1 .5-.5"/></symbol>
            </svg>
            <code class="career-terminal__command"><span class="p-user">$</span> cat /var/log/gumbarros</code>
            <div class="career-terminal__events">
              {(t('about.events') as readonly { year: string; text: string }[]).map((event, index) => (
                <div class="career-event">
                  <span class="career-event__year">[{event.year}]</span>
                  <svg class="career-event__icon"><use href={`#career-icon-${index % 2}`} /></svg>
                  <span>{event.text}</span>
                </div>
              ))}
            </div>
          </div>
        </div>

      </div>
    </div>
  </section>

  <!-- ============================ CONTACT ============================ -->
  <section class="contact" id="contact">
    <div class="wrap contact__inner">
      <div class="contact__text">
        <code class="contact__head"><span class="p-user">gumbarros</span><span class="p-path">@portfolio:~$</span> {t('contact.cmd')}</code>
        <h2 class="reveal">{t('contact.headline')}</h2>
        <div class="contact__ctas reveal">
          <a class="btn btn--primary" href="https://www.linkedin.com/in/gumbarros" target="_blank" rel="noopener">
            <span>{t('contact.linkedin')}</span><span class="arrow">↗</span>
          </a>
          <a class="btn btn--ghost" href="https://github.com/gumbarros" target="_blank" rel="noopener">
            <span>{t('contact.github')}</span><span class="arrow">↗</span>
          </a>
        </div>
      </div>

      <figure class="contact__photo reveal">
        <div class="codebox__bar">
          <span>{t('contact.photo')}</span>
        </div>
        <img class="avatar" src="/me.png" alt="Gustavo Mauricio de Barros" width="460" height="460" loading="lazy" decoding="async" />
      </figure>
    </div>
  </section>
</SiteLayout>

<script>
  // Hero type-in (one-shot, gated behind reduced-motion)
  const cmd = document.querySelector<HTMLElement>('[data-type]');
  if (cmd) {
    const full = cmd.dataset.type as string;
    if (matchMedia('(prefers-reduced-motion: reduce)').matches) {
      cmd.textContent = full;
    } else {
      cmd.textContent = '';
      let i = 0;
      const tick = () => {
        cmd.textContent = full.slice(0, ++i);
        if (i < full.length) setTimeout(tick, 120);
      };
      setTimeout(tick, 400);
    }
  }
</script>
